---
name: pn_single 架构报告
overview: 为 `pn_single.py` 生成一份架构报告，包含模块职责、调用链分析和 Mermaid 架构图，便于理解单设备 Petri 网调度核心逻辑及其上下游依赖。
todos: []
isProject: false
---

# pn_single.py 架构报告

## 1. 模块概述

**文件**：`[solutions/Continuous_model/pn_single.py](solutions/Continuous_model/pn_single.py)`

**定位**：单设备 Petri 网（构网驱动、单机械手、单动作）的核心仿真引擎，实现连续时间 Petri 网调度逻辑。

**执行链**（模块头部注释）：`construct_single -> get_action_mask -> step -> calc_reward`

---

## 2. 依赖关系图

```mermaid
graph TB
    subgraph External [外部配置与数据]
        PetriEnvConfig[PetriEnvConfig]
        single_json[single.json / cascade.json]
    end

    subgraph Construct [构网层]
        construct_single[construct_single.build_single_device_net]
        parse_route[parse_route]
        ROUTE_SPECS[ROUTE_SPECS]
    end

    subgraph Core [pn_single 核心]
        ClusterTool[ClusterTool]
        REASON_DESC[REASON_DESC]
    end

    subgraph DataModel [数据模型]
        Place[pn.Place + SR/TM/PM/LL]
        BasedToken[construct.BasedToken]
    end

    subgraph Helper [辅助函数]
        helper_function[helper_function]
    end

    subgraph Consumers [消费者]
        Env_PN_Single[Env_PN_Single]
        petri_adapter[petri_single_adapter]
        tests[tests]
    end

    PetriEnvConfig --> ClusterTool
    single_json --> PetriEnvConfig
    construct_single --> ClusterTool
    parse_route --> construct_single
    ROUTE_SPECS --> parse_route
    Place --> ClusterTool
    BasedToken --> ClusterTool
    helper_function --> ClusterTool
    ClusterTool --> Env_PN_Single
    ClusterTool --> petri_adapter
    ClusterTool --> tests
```



---

## 3. 类与核心结构

### 3.1 ClusterTool 类


| 职责  | 说明                                                                             |
| --- | ------------------------------------------------------------------------------ |
| 初始化 | 从 `PetriEnvConfig` 加载配置，调用 `build_single_device_net()` 构建网，解析路由元数据             |
| 状态  | `pre/pst/net/m0/m/k/ptime`（矩阵）、`marks`（Place 列表）、`time`、`fire_log` 等           |
| 动作  | `step()`、`reset()`、`get_action_mask()`（使能与 wait 规则在此统一，直接写 mask；`reset()` 返回的 transition 列表来自 `_get_enable_t()`） |
| 观测  | `get_obs()`、`get_obs_dim()`；`step()` 返回 `(done, reward, scrap, action_mask, obs)`；观测构造由 `Place.get_obs` 承担，ClusterTool 仅负责 `_get_obs_place_order` 确定观测顺序并依次调用各库所 `place.get_obs()` 聚合 |
| 奖励  | `calc_reward()`；`blame_release_violations()` 仅在 episode 结束后由训练脚本调用                                   |
| 清洗  | `_init_cleaning_state`、`_advance_cleaning_and_idle`、`_on_processing_unload`    |


### 3.2 Place 类继承结构

库所特征通过 Place 子类实现，各子类在构造时接收类型特定参数，覆写 `get_obs()` 返回对应观测向量。

```mermaid
classDiagram
    class Place {
        +name: str
        +capacity: int
        +processing_time: int
        +type: int
        +tokens: Deque
        +clone()
        +head()
        +pop_head()
        +append()
        +res_time()
        +get_obs() List~float~
        +get_obs_dim() int
    }
    class SR {
        +_n_wafer: int
        +get_obs() List~float~
    }
    class TM {
        +_D_Residual_time: int
        +_target_onehot_map: Dict
        +_onehot_dim: int
        +get_obs() List~float~
    }
    class PM {
        +_P_Residual_time: int
        +_cleaning_duration: int
        +_cleaning_trigger_wafers: int
        +_scrap_clip_threshold: float
        +get_obs() List~float~
    }
    class LL {
        +get_obs() List~float~
    }
    Place <|-- SR
    Place <|-- TM
    Place <|-- PM
    Place <|-- LL
```

| 子类 | 库所 | 特征维度 | 构造参数 |
|-----|------|---------|---------|
| **SR** (Source) | LP, LP_done | LP: 1 维；LP_done: 0 维（不进 obs） | n_wafer |
| **TM** (Transport) | d_TM1, d_TM2, d_TM3 | 4 时间 + 4/2 one-hot | D_Residual_time, target_onehot_map, onehot_dim |
| **PM** (Process Module) | PM1, PM3, PM4, PM6, PM7... | 9 维 | P_Residual_time, cleaning_* |
| **LL** (Load Lock) | LLC, LLD | 4 维 | 无额外参数 |

构网时由 `construct_single.build_single_device_net(obs_config=...)` 根据 `obs_config` 创建对应子类实例；`ClusterTool` 传入 `obs_config` 后，`marks` 中为 PM/TM/LL/SR 实例，`_clone_marks` 通过 `place.clone()` 保持子类类型。

### 3.3 常量

- `CHAMBER=1`、`DELIVERY_ROBOT=2`、`SOURCE=3`：库所类型
- `REASON_DESC`：动作不使能原因的人性化描述（供 Markdown 报告、可视化使用）

---

## 4. 主要调用链

### 4.1 初始化链

```mermaid
sequenceDiagram
    participant Env
    participant ClusterTool
    participant construct_single
    participant parse_route

    Env->>ClusterTool: __init__(config)
    ClusterTool->>parse_route: parse_route(stages, BUFFER_NAMES)
    Note over ClusterTool: 解析 chambers, u_targets, step_map, release_chain_by_u 等
    ClusterTool->>construct_single: build_single_device_net(...)
    construct_single-->>ClusterTool: pre, pst, m0, marks, id2p_name, id2t_name, ...
    ClusterTool->>ClusterTool: _clone_marks, _refresh_episode_proc_time
    ClusterTool->>ClusterTool: _init_cleaning_state
```



### 4.2 step 执行链（非 wait）

```mermaid
sequenceDiagram
    participant Env
    participant ClusterTool

    Env->>ClusterTool: step(a1=transition_idx)
    Note over ClusterTool: do_wait=False
    ClusterTool->>ClusterTool: _advance_and_compute_reward(dt, t1, t2)
    Note over ClusterTool: 单次 marks 遍历完成 reward + stay_time 推进 + cleaning + scrap/qtime
    ClusterTool->>ClusterTool: _fire(t_idx, t1, t2)
    ClusterTool->>ClusterTool: get_action_mask（遍历 marks，内联 ready_chamber 检查）
    ClusterTool-->>Env: DONE, reward, SCRAPE, action_mask
```



### 4.3 step 执行链（wait）

```mermaid
sequenceDiagram
    participant Env
    participant ClusterTool

    Env->>ClusterTool: step(wait_duration=N)
    Note over ClusterTool: do_wait=True
    ClusterTool->>ClusterTool: get_next_event_delta（扫描 _token_pool，按运输/加工库所与节拍计算）
    Note over ClusterTool: 截断到 min(requested_wait, next_event_delta) 或 5s
    ClusterTool->>ClusterTool: _advance_and_compute_reward(dt, t1, t2)
    Note over ClusterTool: 单次 marks 遍历完成 reward + stay_time 推进 + cleaning + scrap/qtime
    ClusterTool->>ClusterTool: get_action_mask（遍历 marks，内联 ready_chamber 检查）
    ClusterTool-->>Env: DONE, reward, SCRAPE, action_mask
```



### 4.4 使能计算链

```mermaid
flowchart TD
    get_action_mask[get_action_mask 使能判定]
    stage1[Stage1: 基础使能]
    stage2[Stage2: 就绪过滤]
    locked[双臂: 锁定 d_TM1 队首 dst 层]
    base_check[pre/pst + 容量 + color_pre]
    u_check[u_*: _select_target_for_source]
    t_check[t_*: 队首 _target_place 匹配]
    proc_ready[u_*: _is_process_ready]
    dwell_check[t_*: dwell_time, is_cleaning]

    get_action_mask --> stage1
    stage1 --> locked
    stage1 --> base_check
    base_check --> u_check
    base_check --> t_check
    stage1 --> stage2
    stage2 --> proc_ready
    stage2 --> dwell_check
```



### 4.5 _fire 内部链

```mermaid
flowchart TD
    _fire["_fire(t_idx)"]
    pre_place[pre_place.pop_head]
    track_leave[_track_leave]
    u_branch{u_*?}
    _select_target[_select_target_for_source]
    _on_processing[_on_processing_unload]
    track_enter[_track_enter]
    pst_place[pst_place.append]
    update_m[更新 self.m]

    _fire --> pre_place
    pre_place --> track_leave
    _fire --> u_branch
    u_branch -->|是| _select_target
    u_branch -->|是| _on_processing
    u_branch -->|否 t_*| track_enter
    track_leave --> pst_place
    track_enter --> pst_place
    pst_place --> update_m
```



---

## 5. 模块交互架构图

```mermaid
flowchart TB
    subgraph Init [初始化]
        config[PetriEnvConfig]
        build_net[build_single_device_net]
        parse[parse_route]
        clone[_clone_marks]
    end

    subgraph State [状态维护]
        marks[marks: Place]
        m[m: token 计数]
        time[time]
        fire_log[fire_log]
    end

    subgraph Enable [使能判定]
        get_action_mask[get_action_mask]
        get_enable_actions_with_reasons[get_enable_actions_with_reasons]
    end

    subgraph Step [步进执行]
        step[step]
        _fire[_fire]
        advance_time[advance_time]
        calc_reward[calc_reward]
    end

    subgraph Reward [奖励与追责]
        _check_scrap[_check_scrap]
    end

    config --> build_net
    parse --> build_net
    build_net --> marks
    clone --> marks

    marks --> get_action_mask
    marks --> step
    step --> get_action_mask
    step --> _fire
    step --> advance_time
    step --> calc_reward
    step --> _check_scrap
```



---

## 6. 方法调用关系汇总


| 入口方法                         | 直接调用                                                                                                         | 间接调用                                                 |
| ---------------------------- | ------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------- |
| `step(a1, wait_duration)`    | `_advance_and_compute_reward`, `_fire`, `get_action_mask` | `get_next_event_delta`, `_get_place` |
| `reset()`                    | `_clone_marks`, `_refresh_episode_proc_time`, `_get_enable_t`                                                | `_get_place`                                         |
| `get_action_mask()`          | 遍历 `marks`（非 `_token_pool`），内联 remaining_time / ready_chamber 检查，缓存 `_select_target_for_source` 结果；内部调用 `_transition_structurally_enabled` | -                                                    |
| `_advance_and_compute_reward(dt, t1, t2)` | 单次 marks 遍历合并：reward 计算 + `stay_time` 推进 + 清洗/idle 推进 + scrap/qtime 检测 | - |
| `_fire(t_idx)`               | `_track_leave`, `_track_enter`, `_select_target_for_source`, `_on_processing_unload`, `_next_robot_machine`  | `_get_place`                                         |
| `calc_reward(t1, t2)`        | **已废弃**，由 `_advance_and_compute_reward` 内联替代                                                             | -                                                    |
| `advance_time(dt)`           | **已废弃**，由 `_advance_and_compute_reward` 内联替代                                                             | -                                                    |
| `blame_release_violations()` | `_chamber_timeline`, `_release_station_aliases`, `_release_chain_by_u`                                       | -                                                    |


---

## 7. 主链接口约束（mask 优先）

- `ClusterTool.step` 当前统一返回 4 元组：`(done, reward_result, scrap, action_mask)`。
- 训练主链（`Env_PN_Single._step`）直接消费 `step` 返回的 `action_mask`，不再二次构造。
- `enable` 列表仅作为调试/评估可解释信息保留（如 `get_enable_actions_with_reasons`）。

## 8. 文档输出建议

建议将以上内容输出为独立文档：`docs/pn_single_architecture.md`。文档结构可包含：

- Abstract（What/When/Not）
- 模块概述
- 依赖关系
- 调用链（含 Mermaid 图）
- 类与核心方法说明
- Related Docs 链接（pn_api.md、continuous_solution_design.md、env_place_obs.md）

---

### Docs consulted

- `[docs/README.md](docs/README.md)` 文档索引
- `[docs/pn_api.md](docs/pn_api.md)` ClusterTool / PetriSingleDevice API
- `[docs/continuous_solution_design.md](docs/continuous_solution_design.md)` 单设备扩展设计

### Derived constraints

- 执行链固定为 construct_single -> get_action_mask（使能）/ step -> _advance_and_compute_reward
- 使能判定在 get_action_mask 中完成，遍历 marks 而非 _token_pool
- 仅 `u_LP`、`u_LLC`、`u_LLD` 参与 blame_release_violations
- WAIT 存在“加工完成待取片”时仅允许 5s 档位

